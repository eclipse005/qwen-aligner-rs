/target

# Model weights (1.2GB, downloaded at runtime)
/models/

# Run-output JSONs (regenerated on every align run)
/test_output.json
/candle_output.json

# Local benchmark / debug output — regenerated by every bench run.
/bench_outputs/

# Test fixtures (audio/text) and any integration tests — local-only.
/tests/
